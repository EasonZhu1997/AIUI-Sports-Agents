# 隐私原则

AIUI Sports Agents 采用“本地优先、最少数据、目的限定、可验证退出”的默认策略。本文件约束总项目、参考实现和公开证据；具体运动项目可以增加更严格的规则，但不能静默降低这里的基线。

## 默认行为

- 源码仓库本身不运行分析遥测，也不要求云账号才能阅读、构建或执行本地测试。
- 参考应用应默认离线完成可离线的训练、指标与总结流程。
- 网络上传必须是针对一个清晰目的的显式 opt-in，不能通过安装、进入页面、连接传感器或接受通用条款来推定同意。
- 关闭上传后，不得继续创建新的网络队列；已有待传数据必须向用户说明并提供保留或删除选择。
- 网络失败不能抹掉本地总结，也不能把“未上传”显示为“已同步”。

## 数据分类

| 类别 | 例子 | 默认处理 |
| --- | --- | --- |
| 实时传感器 | BLE 通知、IMU 帧、心率、踏频、桨频 | 内存中处理；不默认上传原始包或逐帧数据 |
| 派生运动指标 | 时长、距离、配速、速度、功率、覆盖率 | 本地最小化保存；上传须显式开启并限定字段 |
| 设备元数据 | 名称、地址、序列号、固件、host build | 连接所需信息只在必要范围使用；公开证据必须脱敏 |
| 身份与凭据 | 账号、令牌、owner 标记 | 不进入日志、测试夹具、截图或仓库 |
| 诊断与证据 | 日志、录屏、照片、结果卡 | 先最小化与脱敏；公开前单独审核 |

除非某个独立功能明确需要、提供单独说明并获得显式同意，否则不得采集或上传位置、轨迹、通讯录、麦克风录音、相机内容、原始 BLE/IMU 数据、设备唯一标识或可识别个人的健康资料。

## 一个合格的上传开关

启用前必须在用户可见界面说明：

1. 功能名称和目的；
2. 发送哪些字段、不发送哪些字段；
3. 接收方与目标服务；
4. 触发时机、重试与本地队列行为；
5. 保存期限和删除方式；
6. 如何再次关闭。

同意应紧邻功能触发点，不预勾选，不用拒绝即退出的方式强迫同意。改变目的、扩大字段或更换接收方时必须重新征得同意。

## 本地保存与删除

- 只保存恢复会话、生成总结或调试已选择功能所必需的数据。
- 为每类本地记录设置数量、大小或时间上限；损坏或归属不明的数据应 fail closed，不得跨用户合并。
- 用户应能从应用内清除本地历史、待上传队列和诊断记录；若运行环境暂不支持，项目文档必须明确限制和替代步骤。
- 删除失败必须可见，不能用空界面冒充删除成功。

## 公开测试与硬件证据

公开仓库只接收经过审核的摘要和必要片段。提交前移除姓名、账号、位置、轨迹、时间模式、设备地址/序列号、通知内容、环境声音和无关人员画面。真实设备照片、录屏、厂商素材和日志不因附在 issue 或 PR 中而自动适用 Apache License 2.0、PolyForm Noncommercial 或商业许可；其权利、许可作用域与相关人员同意必须单独确认。

用于公开比较的数据应尽量使用合成夹具。确需真人训练数据时，应取得明确同意，说明公开范围，并允许在不破坏已发布软件完整性的前提下撤下原始个人材料。聚合结果不能被用于医疗诊断或推断个人疾病。

## 贡献者要求

涉及新数据字段、权限、本地保存、网络端点、后台任务或日志的 PR 必须附数据流说明，列出收集、处理、保存、上传、删除与失败状态。维护者可以要求隐私评审，且不得只凭代码中“匿名”字样认定数据不可识别。

发现可能的数据泄漏时，请按 [SECURITY.md](./SECURITY.md) 私下报告，不要在公开 issue 中粘贴样本数据。
