# 安全策略

AIUI Sports Agents 会接触运动传感器、蓝牙链路、可穿戴设备和可能的网络同步。安全问题同时包括传统软件漏洞、隐私泄漏和可能导致现实设备误动作的产品缺陷。

## 私下报告

请优先使用代码托管平台的 **Private vulnerability reporting / Report a vulnerability** 功能。若该入口尚未启用，请通过维护者公开资料中的非公开联系方式请求一个私密报告通道；必要时可以创建一个只写“需要安全联系通道”的最小 issue，但不要在 issue 中附漏洞细节、个人数据、设备标识或利用代码。

维护者不得要求报告者先公开漏洞才能受理。未经双方同意，也不要求在修复发布前公开时间线、复现步骤或受影响设备信息。

报告请尽量包含：

- 受影响项目、commit、版本和运行环境；
- 影响与可达到的最小复现条件；
- 是否涉及账号、位置、设备控制或人身安全；
- 已做的脱敏以及可安全共享的日志；
- 建议的缓解方式（若有）。

不要发送真实密钥、完整账号数据、未脱敏的运动轨迹、MAC 地址、序列号或包含旁人的录音录像。维护者需要更多材料时，应先约定安全的传递方式。

## 受理范围

包括但不限于：

- 密钥、凭据、身份或设备标识泄漏；
- 未经同意的网络请求、遥测、日志上传或跨用户数据混淆；
- 恶意 BLE 包导致崩溃、越界解析、状态污染或错误指标；
- 连接代次、重连、隐藏恢复或迟到回调造成错误设备归属；
- 构建、依赖、发布物或校验链被篡改；
- 将估算/缺失数据误报为实测，进而产生不安全训练提示；
- 未经明确用户操作的设备控制、自动连接或持久化控制；
- Rower 访问或写入 FTMS Control Point `0x2AD9`。

一般性的功能缺陷、文档问题和不包含敏感细节的兼容性问题可以走普通 issue。

## 响应目标

维护者以以下目标处理私密报告，但这些是尽力目标，不是服务等级承诺：

- 7 个自然日内确认收到；
- 14 个自然日内完成初步影响判断；
- 在可用缓解措施明确后协调修复、公告和披露时间。

如果问题存在现实伤害风险，优先提供停用、回滚或离线缓解措施。修复发布前不会把“正在调查”表述成“已安全”。

## 支持范围

首个公开版本发布前，安全修复只针对当前默认分支。发布后，默认支持当前默认分支与最新公开版本；更早版本是否回补由公告逐项说明。孵化项目可能只获得尽力支持，但同样遵守私密报告和数据保护规则。

## 安全研究约定

善意研究应使用自有设备和测试数据，避免影响他人服务或设备，不进行社会工程、数据外传、拒绝服务或现实机器的危险控制。请只保留证明问题所需的最少数据并及时删除。遵守这些边界的报告者不会仅因善意报告而被要求公开敏感材料。
